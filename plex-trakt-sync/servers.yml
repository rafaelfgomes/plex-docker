servers:
  default:
    token: ${PLEX_TOKEN}
    urls:
      - http://${PLEX_IP}:32400
    config:
      libraries:
        - "Seriados"
        - "Filmes"
        - "Animes"
      sync:
        plex_to_trakt:
          ratings: true
          watched_status: true
        trakt_to_plex:
          ratings: true
          watched_status: true